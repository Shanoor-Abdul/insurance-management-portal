# Local MongoDB (default)
MONGODB_URI=mongodb://localhost:27017/insurance_portal

# MongoDB Atlas example (make sure special characters in the password are URL-encoded, e.g. @ becomes %40)
# MONGODB_URI=mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/insurance_portal
